<template>
  <!-- Form -->
  <div class="auth-panel auth-panel--right">
    <div class="auth-card">
      <div class="auth-card__header">
        <h1 class="auth-card__title">Create Account</h1>
        <p class="auth-card__subtitle">Start building your pages today</p>
      </div>

      <div class="auth-card__body">
        <!-- Name field -->
        <div class="form-group">
          <label class="form-label" for="name">Full Name</label>
          <input
            class="form-input"
            type="text"
            id="name"
            name="name"
            placeholder="John Doe"
            autocomplete="name"
          />
        </div>

        <!-- Email field with error example -->
        <div class="form-group">
          <label class="form-label" for="email">Email Address</label>
          <input
            class="form-input form-input--error"
            type="email"
            id="email"
            name="email"
            placeholder="you@example.com"
            autocomplete="email"
          />
          <span class="form-error">
            <svg
              width="13"
              height="13"
              viewBox="0 0 13 13"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
            >
              <circle cx="6.5" cy="6.5" r="5.5" stroke="#c0392b" stroke-width="1.5" />
              <path d="M6.5 4v3" stroke="#c0392b" stroke-width="1.5" stroke-linecap="square" />
              <circle cx="6.5" cy="9" r="0.7" fill="#c0392b" />
            </svg>
            This email address is already registered.
          </span>
        </div>

        <!-- Password field -->
        <div class="form-group">
          <label class="form-label" for="password">Password</label>
          <input
            class="form-input"
            type="password"
            id="password"
            name="password"
            placeholder="••••••••"
            autocomplete="new-password"
          />
          <span class="form-hint">Minimum 8 characters</span>
        </div>

        <button class="btn btn--primary btn--full" type="button">Create Account</button>
      </div>

      <div class="auth-card__footer">
        <span>Already have an account?</span>
        <a href="login.html">Sign in</a>
      </div>
    </div>
  </div>
</template>
